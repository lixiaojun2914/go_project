version: "3.5"
services: 
    web:
        build: .
        command: ./test
        ports:
            - "80:8080"
        networks: 
            - mynet
        volumes:
            - type: volume
              source: myvol
              target: /code
networks: 
    mynet:
volumes: 
    myvol: