version: "3.5"
services: 
    web:
        build: .
        command: ./test
        ports:
            - target: 80
              published: 8080
        networks: 
            - mynet
        volumes:
            - type: volume
              source: myvol
              target: /code
networks: 
    mynet:
volumes: 
    myvol: